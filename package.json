{
  "name": "arb-funhouse-contracts",
  "version": "1.0.0",
  "description": "ARB Fun House smart contracts — Arbitrum Sepolia",
  "type": "module",
  "scripts": {
    "compile": "npx hardhat compile",
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network arbitrumSepolia",
    "deploy:local": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^4.0.7",
    "@nomicfoundation/hardhat-verify": "^3.0.13",
    "dotenv": "^16.0.0",
    "ethers": "^6.16.0",
    "hardhat": "^3.3.0"
  },
  "dependencies": {
    "@chainlink/contracts": "^1.3.0",
    "@openzeppelin/contracts": "^5.0.0",
    "@supabase/supabase-js": "^2.102.1"
  }
}
